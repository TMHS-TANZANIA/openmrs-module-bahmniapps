pipeline {

    agent any

    stages {
        
        stage("build") {
            
            steps {
                echo 'Build from master branch'
            }
        }

        stage("deploy") {
            
            steps{
                echo 'Deploy project to the server'
            }
        }

    }
}
